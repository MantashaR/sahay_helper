# secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem

# Python
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.venv/
venv/
env/

# editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# dev logs
server.out
server.err

# unrelated nested project (own git/venv/secret)
byoc-workshop-template/
_vidframes/

# built docker image artifact (large)
*.tar
sahay-image.tar

# pitch deck — kept local, not part of the code repo
*.pptx

.vercel
