# Large build artifact — never upload (101 MB Docker image)
*.tar
sahay-image.tar

# Unrelated nested project with its OWN secrets (.env) — must never leak
byoc-workshop-template/

# Secrets
.env
.env.*
!.env.example
*.key
*.pem

# Python caches / envs
__pycache__/
*.pyc
.venv/
venv/

# Build / video scratch
_vidframes/

# Heavy + irrelevant to the running web app
*.pptx
Dockerfile
docker-compose.yml
.dockerignore
DOCKER-IMAGE-README.txt
fill_ppt.py
inspect_ppt.py
verify_ppt.py
verify_out.txt
README.txt
